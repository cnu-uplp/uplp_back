# 도커 빌드 컨텍스트에서 제외할 것.
# Dockerfile이 `COPY . .` 이라, 이 파일이 없으면 아래가 전부 이미지에 구워진다.
#   venv/  — 149MB (이미지 크기·빌드 시간)
#   .env   — 비밀키가 이미지 레이어에 박힌다. 환경변수는 실행 시 --env-file로 주입한다.
venv/
.venv/
.env
.git/
.gitignore
__pycache__/
*.pyc
.idea/
.claude/
